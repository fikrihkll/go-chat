{
    // Use IntelliSense to learn about possible attributes.
    // Hover to view descriptions of existing attributes.
    // For more information, visit: https://go.microsoft.com/fwlink/?linkid=830387
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Launch Package",
            "type": "go",
            "request": "launch",
            "mode": "auto",
            "program": "${workspaceFolder}/cmd/api", // Path to the folder containing main.go
            "cwd": "${workspaceFolder}", // Set the working directory to the root
            "envFile": "${workspaceFolder}/.env", // set the env path
            "args": [], // Add any CLI arguments here, if required
            "buildFlags": "", // Add build flags if needed
        }
    ]
}